interface Movement {
    horizontalMovement: number;
    verticalMovement: number;
}

interface Position {
    initialX: number;
    initialY: number;
}

interface MapCoordinates {
    x: number;
    y: number;
    z: number;
}
